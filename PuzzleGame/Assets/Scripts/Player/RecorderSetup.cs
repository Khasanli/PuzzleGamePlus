
namespace Photon.Voice.Unity
{
    using UnityEngine;

    public class RecorderSetup : MonoBehaviour
    {
        [SerializeField]
        private Recorder recorder;

        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
